name: Temporary Recovery Frontier Single Flight Patch

on:
  push:
    branches: [main]
    paths:
      - '.github/workflows/temp_frontier_singleflight_patch.yml'

permissions:
  contents: write

jobs:
  patch:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - name: Apply single-flight patch
        run: python3 tmp_frontier_singleflight_patch.py
      - name: Commit source changes
        shell: bash
        run: |
          git config user.name "tejas-shivanand-mane"
          git config user.email "38262749+tejas-shivanand-mane@users.noreply.github.com"
          git add src/ray/core_worker/core_worker.h \
                  src/ray/core_worker/recovery_frontier_publication.cc
          git diff --cached --check
          git commit -m "Coalesce Recovery Frontier publication waiters [skip ci]"
          git push origin HEAD:main
